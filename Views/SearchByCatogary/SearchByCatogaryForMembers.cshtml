@{
    Layout = "~/Views/Shared/_LayoutForMembers.cshtml";
}
@model List<QuotesProj.Models.Quotes>
<h1>Search By Catogary</h1>
<form action="/SearchByCatogary/Search2" method="post">
    <input type="text" placeholder="Enter Catogary" id="n1">
    <input type="button" value="Search" id="b1">
</form>
<br />
<br />
<br />
<div>
    <div style="display:block;">
        <div>
            <div>&nbsp;</div>
            <ul id="hide"></ul>
        </div>
    </div>
</div>
@Scripts.Render("~/bundles/jquery")
<script>

    $(document).ready(function () {

        $('#hide').hide();

        $('#b1').click(function () {

            //alert('User Name =');
            $('#hide').show();
            $.getJSON("/SearchByCatogary/Search2?cat=" + $('#n1').val(), function (data) {
                $.each(data, function (i, item) {
                    //  $('#hide').append('<li><div class="image"> <a href="#"><img src="http://localhost:5263/' + item.path + '" alt="" /></a> </div><p> Item Number: <span>' + item.PId + '</span><br />Size List : <span>' + item.discription + '</span><br />Catogary: <a href="#">' + item.catogary + '</a> </p><p class="price">Wholesale Price: <strong>' + item.price + '</strong></p></li>');
                    $('#hide').append('<tr><td>' + item.quote_text + '</td><td>' + item.author_name + '</td></tr><br/>');
                    // $('#hide').append('<p> Item Number: <span>' + item.PId + '</span><br />');
                    //$('#hide').append('Size List : <span>' + item.discription + '</span><br /></li>');

                });
            });

        });

    });
</script>